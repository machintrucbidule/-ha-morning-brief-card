# morning-brief-card

> Lovelace custom card that renders the canonical JSON produced by the `home-assistant-morning-brief` integration.

This is a stub. The full README is produced in Phase 11 of the implementation plan (Section 36 of `MORNING_BRIEF_SPEC.md`).

## Status

Pre-release. Implementation has not started — waits until the backend Phase 9 (services + sensor entity) is complete.

## Companion

This card depends on the `home-assistant-morning-brief` integration. Install both via HACS.
